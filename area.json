{
  "$schema": "https://vega.github.io/schema/vega-lite/v5.json",
  "title": "Enrolment by Education Level",
  "data": {
    "url": "https://raw.githubusercontent.com/koohyejin0127/FIT3179_A2/refs/heads/main/data/enrolment_filtered.csv"
  },
  "params": [{"name": "state_select", "value": "Malaysia"}],
  "transform": [
    {"filter": "datum.date == '1/1/2022'"},
    {"filter": "datum.state != 'Malaysia'"},
    {"filter": "state_select == 'Malaysia' ? true : datum.state == state_select"},
    {
      "calculate": "datum.stage == 'primary' ? 1 : datum.stage == 'secondary' ? 2 : 3",
      "as": "xpos"
    }
  ],
  "mark": {"type": "area"},
  "encoding": {
    "x": {
      "field": "xpos",
      "title": "Education Level",
      "type": "quantitative",
      "axis": {
        "values": [1, 2, 3],
        "labelExpr": "datum.value == 1 ? 'Primary' : datum.value == 2 ? 'Secondary' : 'Tertiary'"
      }
    },
    "y": {
      "field": "students",
      "type": "quantitative",
      "title": "Number of Students",
      "scale": {"domain": [0, 3000000]}
    },
    "color": {
      "field": "state",
      "type": "nominal",
      "title": "State",
      "scale": {"scheme": "category20b"}
    },
    "tooltip": [
      {"field": "state", "title": "State"},
      {"field": "edu_level", "title": "Education Level"},
      {"field": "students", "title": "Number of Enrolment"}
    ]
  },
  "config": {}
}
